<!-- Probably move the stylesheet to you layout. Also make sure you include the javascript. -->
<%= stylesheet_link_tag "event_calendar" %>
<%= javascript_include_tag "event_calendar" %>

<div class="white">
	<div id="section_header"><%=t(:calendar)%></div>
	<div style="margin:0 15px 40px 15px;"><%= t(:calendar_welcome) %><%= link_to t(:add_event), new_event_path(), :class => "btn btn-large btn-afegir" %></div>
	
<%= raw(event_calendar) %>
</div>
